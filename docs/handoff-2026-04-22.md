# JCL GovCon Session Handoff — 2026-04-22

**Repo:** `/Users/joelaptin/jcl-govcon`  
**Branch:** `main`  
**HEAD:** `03cf24d` (`docs: record verified Codex compatibility findings`)  
**Prior operational handoff:** `docs/handoff-2026-04-21.md`

## Source Of Truth

Use this order for re-entry:

1. Live user instructions in the next Codex session
2. This handoff
3. `docs/handoff-2026-04-21.md`
4. Local git state / working tree
5. `plan.md`
6. `progress.md` only when it does not conflict with newer handoffs or local state

## Current Git State

Working tree is **dirty** on `main`. `origin/main` also points at `03cf24d`, so nothing in this session has been committed or pushed yet.

Current dirty tree includes a mixed local feature bundle:

- contract triage UI/API work in:
  - `src/app/api/contracts/route.ts`
  - `src/app/api/contracts/[id]/route.ts`
  - `src/components/contract-detail.tsx`
  - `src/components/kanban/board.tsx`
  - `src/components/kanban/card.tsx`
  - `src/components/kanban/column.tsx`
  - `src/components/sidebar.tsx`
- new archive/watch surfaces in:
  - `src/app/archive/`
  - `src/app/watch/`
  - `src/app/api/watch-targets/`
  - `src/app/api/cron/watch-check/`
  - `src/lib/watch/`
- local infra/doc tweaks in:
  - `railway.weekly-crawl.json`
  - `railway.check-batches.json`
  - `docs/deployment-railway.md`
- tracker docs in:
  - `plan.md`
  - `progress.md`

Do **not** assume every dirty file belongs to one clean PR-sized unit. The local tree combines dashboard triage UX, watch workflow work, and a local Friday cron wording/config change.

## What Changed This Session

### 1. Archive support was added to contract triage

- Contract detail page now has an **Archive / Unarchive** button next to the classification / Chosen controls.
- Dashboard Kanban cards now also have an inline **Archive** button.
- Manual archive is stored as an `ARCHIVED` tag, not a schema change.
- `/api/contracts` now supports:
  - `archived=true`
  - `includeArchived=true`
  - `expired=true`
  - `includeExpired=true`
- Default active views now hide:
  - manually archived contracts
  - expired contracts
- New `/archive` page shows expired + manually archived contracts together.
- Sidebar now has an **Archive** nav item with a badge count.
- Pipeline page explicitly requests `includeExpired=true` so pipeline rows do not disappear there.

### 2. Watched contracts were separated from the main dashboard

- Main homepage Kanban fetches now include `watched=false`.
- Result: once a contract is actively being watched, it should leave the root dashboard columns even if it still keeps its underlying `GOOD` classification.
- Watch work now has its own local surfaces in the tree:
  - `/watch`
  - `/watch/[id]`
  - `/api/watch-targets`
  - `/api/cron/watch-check`
- Treat watch as an orthogonal workflow, not a replacement for classification.

### 3. Promoted contracts were separated from the main dashboard

- Main homepage Kanban fetches now include `promoted=false`.
- Result: once a contract is marked **Chosen/Promoted**, it should leave the normal homepage columns and live on `/chosen`.

### 4. Contract cards were rewritten to show better content

- Dashboard cards no longer use `aiReasoning` in the body.
- Cards now prefer the full **"What This Contract Is"** description from `actionPlan.description`, then fall back to `summary`.
- The card description is no longer truncated to a few words.
- Chosen cards render a compact **Analyst Summary** preview from `contracts.notes`.
- `/api/contracts` list payload now includes the fields the cards need:
  - `summary`
  - `actionPlan`
  - `notes`

### 5. Dashboard typography was increased for readability

- The main Kanban card title, agency/ceiling/deadline row, notes preview, and archive button were all bumped up.
- The description block now renders at `text-sm` (14px) instead of the smaller card-copy size.
- This was a user-directed readability change, not a design-system overhaul.

### 6. Tracker docs were refreshed

- `plan.md` now reflects:
  - 51 test files / 454 tests
  - Phase 8.7 as code-merged with Railway provisioning still pending
  - Phase 8.8 for the local dashboard triage bundle
  - Phase 9 now explicitly pointing toward GoHighLevel pipeline work
- `progress.md` now reflects:
  - current local focus is dashboard triage polish on dirty `main`
  - GoHighLevel pipeline discovery is now an immediate next action
- `TODOS.md` now includes a top-priority `P1` item for the GoHighLevel pipeline experiment.

### 7. Weekly scrape schedule was changed locally

Local config/docs now reflect:

- `railway.weekly-crawl.json`: `0 15 * * 5`
- `docs/deployment-railway.md`: Friday 15:00 UTC wording
- UI copy updated in:
  - `src/app/inbox/page.tsx`
  - `src/app/admin/crawl-runs/page.tsx`

This means the intended local schedule is now **Friday 8:00 AM Pacific / 15:00 UTC**, not Monday. Live Railway still needs fresh confirmation before treating that as production truth.

## Contract Analysis Saved This Session

### DFAS on-site media destruction

- **Contract ID:** `b80aeff8-d88c-408c-882c-4d71b0cdb307`
- Saved a detailed analyst note into `contracts.notes`.
- Summary:
  - Real RFQ / solicitation
  - On-site destruction at DFAS Indianapolis
  - 1,353 tapes + 7,891 HDD/SSD units
  - Not paper shredding
  - CMMC Level 1 / SPRS / PIEE matters
  - WAWF invoicing after performance
  - Missing/unclear SOW attachment remains a risk
- User intent during session: treat this as **Promote-worthy**, pending real pricing / vendor support.

### JMPS laptop notice

- **Contract ID:** `95765eb2-8289-46fb-b031-e1078d914b7f`
- User explicitly authorized a live SAM notice check.
- Public SAM notice confirms:
  - **Getac S510**
  - **brand-name only**
  - controlled attachment for the real package
- Public notice does **not** expose:
  - quantity
  - RAM/storage/CPU details
- Saved a note onto the contract saying this is **not ready to promote** from public info alone.

## Verification Run

Earlier in this session, the broader local UI/API bundle passed:

- `npx tsc --noEmit`
- `npm run lint`
- `npm run test:run`

That full run covered **51 files / 454 tests** at the time of the dashboard card-content changes.

After the final readability tweak on the Kanban card and this handoff refresh, the focused verification run was:

- `npx vitest run src/__tests__/components/kanban/card.test.tsx src/__tests__/components/kanban/board.test.tsx`

No live browser pass was done after the font-size tweak.

## Known Risks / Do Not Assume

- `TODOS.md` now contains the GoHighLevel pipeline experiment, but no integration code exists yet. The source-of-truth decision is still open.
- The Friday schedule change is only local until committed/deployed and re-verified in Railway.
- `docs/handoff-2026-04-21.md` remains the infra baseline, but it predates today’s dashboard triage/card-copy changes.
- `plan.md` and `progress.md` are now more current than earlier in the session, but local git state still outranks them.
- The DFAS destruction pricing guidance discussed in chat is **not** encoded as code; only the summary note was persisted on the contract.
- The JMPS laptop notice still has unknown quantity and unknown config because the controlled attachment was not available publicly.
- There are pre-existing React `act(...)` warnings in the test suite; they are noisy but not new blockers.

## Recommended Next Actions

1. Re-open on `main` with the existing dirty tree. Do not discard local changes.
2. Start the first narrow GoHighLevel experiment already captured in `TODOS.md`:
   - what enters GoHighLevel
   - when it enters
   - whether this app or GoHighLevel is the source of truth for stage changes
3. Re-verify Railway dashboard state before assuming cron automation is live:
   - confirm `jcl-govcon-web` is GitHub-linked
   - provision `jcl-govcon-weekly-crawl`
   - provision `jcl-govcon-check-batches`
   - smoke-test `check-batches`
   - confirm the actual live weekly schedule
4. If continuing contract triage, use:
   - `/archive` for stale/expired/manual-archive review
   - `/watch` for active monitored opportunities
   - `/chosen` for promoted opportunities
5. For `b80aeff8-d88c-408c-882c-4d71b0cdb307`, next real work is:
   - get one compliant mobile destruction vendor quote
   - confirm the missing SOW / attachments
6. Leave `95765eb2-8289-46fb-b031-e1078d914b7f` unpromoted unless the controlled package reveals manageable quantity/specs and pricing.
