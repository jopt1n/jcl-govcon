# JCL GovCon Session Handoff — 2026-04-23

**Repo:** `/Users/joelaptin/jcl-govcon`  
**Branch:** `main`  
**HEAD:** `03cf24d` (`docs: record verified Codex compatibility findings`)  
**Origin:** `origin/main` also at `03cf24d`  
**Dirty tree:** yes  
**Staged changes:** none

## Session Summary

This repo is no longer in the clean post-cron-merge state described by
`docs/handoff-2026-04-21.md`. The current local worktree contains a large
uncommitted feature bundle on top of `main`:

1. dashboard triage UX work (`/archive`, `/watch`, card/content/actions/sidebar updates)
2. first-class watch backend work (new schema + service layer + routes + cron route)
3. local Railway cron fixes and schedule changes
4. tracker/doc refreshes
5. working notes in `docs/opportunity-eval.md`
6. a live DB triage operation that moved all active `/inbox` rows onto the
   main dashboard by setting `contracts.reviewed_at`

A fresh agent should treat the current local git state as the real source of
truth for in-progress work, with `docs/handoff-2026-04-22.md` as the best
narrative summary of the dashboard/watch bundle so far. `AGENTS.md` still
points at `docs/handoff-2026-04-21.md`, which is now stale for the dirty-tree
work.

## Source Of Truth

Use this order for re-entry:

1. Live user instructions in the next Codex session
2. This handoff
3. Current local git state / working tree
4. `docs/handoff-2026-04-22.md`
5. `docs/handoff-2026-04-21.md`
6. `plan.md`
7. `progress.md`

`AGENTS.md` still names `docs/handoff-2026-04-21.md` as the operational
baseline. That was correct when written, but the dirty tree now outranks it.

## Current Git State

`git status --short --branch` at handoff time:

```text
## main...origin/main
 M TODOS.md
 M docs/deployment-railway.md
 M plan.md
 M progress.md
 M railway.check-batches.json
 M railway.weekly-crawl.json
 M src/__tests__/api/contracts/id.test.ts
 M src/__tests__/api/contracts/route.test.ts
 M src/__tests__/api/cron/weekly-crawl.test.ts
 M src/__tests__/app/chosen/page.test.tsx
 M src/__tests__/app/inbox/page.test.tsx
 M src/__tests__/components/contract-detail.test.tsx
 M src/__tests__/components/kanban/board.test.tsx
 M src/__tests__/components/kanban/card.test.tsx
 M src/__tests__/components/sidebar.test.tsx
 M src/app/admin/crawl-runs/page.tsx
 M src/app/api/contracts/[id]/route.ts
 M src/app/api/contracts/route.ts
 M src/app/api/cron/weekly-crawl/route.ts
 M src/app/chosen/page.tsx
 M src/app/inbox/page.tsx
 M src/app/page.tsx
 M src/app/pipeline/page.tsx
 M src/components/contract-detail.tsx
 M src/components/crawl-status.tsx
 M src/components/kanban/board.tsx
 M src/components/kanban/card.tsx
 M src/components/kanban/column.tsx
 M src/components/sidebar.tsx
 M src/lib/ai/batch-classify.ts
 M src/lib/db/schema.ts
?? docs/handoff-2026-04-22.md
?? docs/opportunity-eval.md
?? railway.watch-check.json
?? src/__tests__/api/cron/watch-check.test.ts
?? src/__tests__/api/watch-targets/
?? src/__tests__/app/archive/
?? src/__tests__/app/page.test.tsx
?? src/__tests__/app/watch/
?? src/__tests__/components/crawl-status.test.tsx
?? src/__tests__/lib/watch/
?? src/app/api/cron/watch-check/
?? src/app/api/watch-targets/
?? src/app/archive/
?? src/app/watch/
?? src/lib/watch/
```

Recent commits:

- `03cf24d` `docs: record verified Codex compatibility findings`
- `af40432` `docs: add Codex bridge and migration plan`
- `c00fa11` `docs: handoff for 2026-04-21 session — cron architecture shipped, GitHub link + provisioning outstanding`
- `f42f046` `fix: three-service cron architecture (Sedgewick [[cron]] blocks were invalid schema) (#3)`

## Source Docs Read

- `AGENTS.md`
- `CLAUDE.md`
- `plan.md`
- `progress.md`
- `docs/handoff-2026-04-22.md`
- `README.md`
- inbox/dashboard split paths inspected:
  - `src/lib/db/schema.ts`
  - `src/app/api/contracts/route.ts`
  - `src/app/inbox/page.tsx`
  - `src/components/kanban/board.tsx`
- focused diffs in:
  - `railway.check-batches.json`
  - `railway.weekly-crawl.json`
  - `docs/deployment-railway.md`
  - `TODOS.md`
  - `plan.md`
  - `progress.md`
  - `src/lib/db/schema.ts`
  - `src/app/api/contracts/route.ts`
  - `src/app/api/contracts/[id]/route.ts`
  - `src/components/contract-detail.tsx`
  - `src/components/kanban/card.tsx`
  - `src/components/sidebar.tsx`
- new/untracked files inspected:
  - `docs/opportunity-eval.md`
  - `railway.watch-check.json`
  - `src/app/api/watch-targets/route.ts`
  - `src/app/api/watch-targets/[id]/route.ts`
  - `src/app/api/cron/watch-check/route.ts`
  - `src/app/watch/page.tsx`
  - `src/app/watch/[id]/page.tsx`
  - `src/app/archive/page.tsx`
  - `src/lib/watch/core.ts`
  - `src/lib/watch/service.ts`
  - `src/lib/watch/watch-check.ts`

## Work Completed / Present In Dirty Tree

### 1. Dashboard triage workflow bundle is still present locally

Already summarized in `docs/handoff-2026-04-22.md`, and still present in the
dirty tree:

- `/archive` page exists
- main dashboard excludes promoted contracts (`/chosen`) and watched contracts
  (`/watch`)
- archive filters added to `/api/contracts`
- contract detail page has Archive + Watch controls
- Kanban cards show richer content and inline Archive
- sidebar now includes Watch + Archive counts
- chosen cards can render analyst-summary previews from notes

### 2. Watch workflow has grown beyond UI into real backend work

The current dirty tree includes new schema and backend surfaces that were not
fully captured in older handoffs:

- `src/lib/db/schema.ts` now adds:
  - `watch_status` enum
  - `watch_link_type` enum
  - `watch_targets`
  - `watch_target_links`
  - `watch_events`
- new service layer:
  - `src/lib/watch/core.ts`
  - `src/lib/watch/service.ts`
  - `src/lib/watch/watch-check.ts`
- new API/routes:
  - `src/app/api/watch-targets/route.ts`
  - `src/app/api/watch-targets/[id]/route.ts`
  - `src/app/api/cron/watch-check/route.ts`
- new pages:
  - `src/app/watch/page.tsx`
  - `src/app/watch/[id]/page.tsx`

This is no longer just "watched contracts leave the dashboard"; it is becoming
a first-class monitoring subsystem with its own tables, matching logic, events,
and Telegram-driven cron check.

### 3. There is now a third cron config candidate in the worktree

Untracked file:

- `railway.watch-check.json`

Current local config:

- start command posts to `/api/cron/watch-check`
- cron schedule: `0 16 * * *`
- uses the same alpine/curl cron container pattern as the existing Railway
  cron services

This is not documented in the older cron handoffs and has not been verified
live in this pass.

### 4. Local Railway cron fixes are present but not deployed

Current local diffs:

- `railway.check-batches.json` now wraps curl in `sh -c '...'`
- `railway.weekly-crawl.json` now wraps curl in `sh -c '...'`
- `railway.weekly-crawl.json` also changes schedule from Monday to Friday:
  - old: `0 15 * * 1`
  - local now: `0 15 * * 5`
- `docs/deployment-railway.md` was edited to match Friday wording

### 5. Tracker docs are being used to narrate uncommitted work

Dirty tracked docs now say:

- `plan.md`: Phase 8.8 local dashboard triage polish, Phase 9 GoHighLevel
  pipeline direction, 51 files / 454 tests
- `progress.md`: current focus is dashboard triage on dirty `main`
- `TODOS.md`: top `P1` item is the GoHighLevel pipeline experiment for watched
  / chosen contracts

These tracker updates are useful context, but they are uncommitted and should
not be treated as canonical beyond the local worktree.

### 6. Active inbox contracts were bulk-moved to the main dashboard in the live DB

This happened on 2026-04-23 in response to an explicit user request. It was a
database operation, not a repo/code change.

Code-path findings before the update:

- `/inbox` is driven by `contracts.reviewed_at IS NULL`
- the main dashboard defaults to `contracts.reviewed_at IS NOT NULL`
- active views also exclude:
  - manually archived rows
  - expired rows
- the main dashboard additionally requests:
  - `promoted=false`
  - `watched=false`

Live counts before the update:

- `unreviewed_total = 5622`
- `inbox_visible = 3061`
- `expired_unreviewed = 2561`
- `promoted_unreviewed = 0`
- `watched_unreviewed = 0`

The update that was run:

- set `reviewed_at = now()`
- set `updated_at = now()`
- where `reviewed_at IS NULL`
- and not archived
- and deadline is null or still in the future

Rows updated:

- `3061`

Live counts after the update:

- `inbox_visible = 0`
- `unreviewed_total = 2561`
- `reviewed_total = 33106`
- `dashboard_eligible_reviewed = 6017`

Important nuance:

- the remaining `2561` unreviewed rows are expired contracts already filtered
  out of active views
- a fresh agent should not assume `/inbox` count and raw `reviewed_at IS NULL`
  count are the same thing
- do not repeat this bulk review operation unless the user explicitly asks

## Live Railway Diagnosis Already Performed

This session lineage already did live Railway inspection. Key findings from
2026-04-22:

- `jcl-govcon-check-batches` current service status: `CRASHED`
- latest failing log:
  - `2026-04-22T19:31:47Z`
  - `curl: (3) URL rejected: Bad hostname`
- `jcl-govcon-web` current service status: `SUCCESS`
- `jcl-govcon-weekly-crawl` current service status: `SUCCESS`

Important live-state details:

- `check-batches` live deployment metadata still shows the old start command:
  - direct `curl ... "$WEB_BASE_URL/api/cron/check-batches"`
  - no `sh -c` wrapper
- `check-batches` live deployment came from commit `03cf24d`
- `weekly-crawl` live deployment metadata still shows:
  - cron schedule `0 15 * * 1` (Monday)
  - no local Friday change deployed yet

Runtime variable checks (redacted) showed:

- `WEB_BASE_URL` present and valid:
  - `https://jcl-govcon-web-production.up.railway.app`
- `INGEST_SECRET` present

Strong inference from logs + local reproduction:

- the bad-hostname crash is consistent with Railway running the cron start
  command without shell expansion of `$WEB_BASE_URL`
- the local `sh -c` wrapper appears to be the intended fix
- that fix is only local right now; it has not been deployed or re-verified
  live

## Verification Performed

### Read-only discovery in this handoff pass

- `git rev-parse --show-toplevel`
- `git status --short --branch`
- `git branch --show-current`
- `git log --oneline --decorate -n 10`
- `git diff --stat`
- `git diff --name-status`
- `git diff --cached --stat`
- `git diff --cached --name-status`
- `git check-ignore -v -- docs/handoff-2026-04-23.md`
- `git check-ignore -v -- progress.md`

Results:

- repo confirmed: `/Users/joelaptin/jcl-govcon`
- branch: `main`
- no staged changes
- `docs/handoff-2026-04-23.md` is not ignored
- `progress.md` is not ignored

### Live DB inspection + update performed in this handoff pass

With explicit user approval to move inbox contracts, this session also:

- counted active unreviewed vs total unreviewed contracts
- verified that the inbox/dashboard distinction is driven by
  `contracts.reviewed_at`
- bulk-updated `3061` active inbox rows to set `reviewed_at` and `updated_at`
- re-checked counts after the update to confirm `/inbox` active count reached
  `0`

### Prior verification already recorded in repo docs

From `progress.md` / `docs/handoff-2026-04-22.md`:

- `npx tsc --noEmit`
- `npm run lint`
- `npm run test:run`

were reported passing for the dashboard triage bundle at:

- 51 test files
- 454 tests

### Not verified in this handoff pass

- no test/lint/typecheck rerun today
- no browser/UI pass today
- no `drizzle-kit push` verification for the new watch schema
- no deploy/redeploy
- no manual trigger of `check-batches`
- no manual trigger of `watch-check`

## Files Changed Or Intended To Change

No files were changed in this handoff pass except this handoff file.

Current dirty tree is spread across these areas:

### Tracked modified

- trackers/docs:
  - `TODOS.md`
  - `plan.md`
  - `progress.md`
  - `docs/deployment-railway.md`
- Railway cron config:
  - `railway.check-batches.json`
  - `railway.weekly-crawl.json`
- schema/backend/UI/tests:
  - `src/lib/db/schema.ts`
  - `src/lib/ai/batch-classify.ts`
  - `src/app/api/contracts/route.ts`
  - `src/app/api/contracts/[id]/route.ts`
  - `src/app/api/cron/weekly-crawl/route.ts`
  - `src/app/*`
  - `src/components/*`
  - multiple test files

### Untracked

- `docs/handoff-2026-04-22.md`
- `docs/opportunity-eval.md`
- `railway.watch-check.json`
- `src/app/archive/`
- `src/app/watch/`
- `src/app/api/watch-targets/`
- `src/app/api/cron/watch-check/`
- `src/lib/watch/`
- related tests under `src/__tests__/api/cron/watch-check.test.ts`,
  `src/__tests__/app/watch/`, `src/__tests__/app/archive/`,
  `src/__tests__/lib/watch/`, etc.

## Known Gaps, Risks, Blockers, Unverified Assumptions

1. **Mixed dirty tree risk**
   - This is not one clean PR.
   - It mixes dashboard UX, watch-system backend/schema work, Railway cron
     fixes, tracker updates, and working notes.

2. **Watch schema likely needs a DB push**
   - `src/lib/db/schema.ts` now defines new watch tables and enums.
   - No evidence in this pass that `drizzle-kit push` has been run anywhere
     safe.
   - Fresh agent should assume runtime/database mismatch is possible until
     verified.

3. **Live Railway still appears to be on pre-fix cron config**
   - `check-batches` is live-crashed on old direct-curl command.
   - `weekly-crawl` live schedule still appears to be Monday.
   - local Friday/shell-wrapper changes are not yet deployed.

4. **Schedule truth is split**
   - older handoff / AGENTS context: Monday
   - local JSON/docs now: Friday
   - live Railway deployment metadata: still Monday
   - do not assume the local Friday schedule is production truth

5. **New watch cron exists only locally**
   - `railway.watch-check.json` is untracked
   - `/api/cron/watch-check` exists locally
   - no evidence it exists as a Railway service yet

6. **Tracker files are dirty and protected**
   - `plan.md` and `progress.md` are both already modified and protected by
     repo guidance
   - do not casually rewrite them without explicit approval

7. **AGENTS source-of-truth note is stale**
   - `AGENTS.md` still names `docs/handoff-2026-04-21.md`
   - current dirty-tree state is better explained by `docs/handoff-2026-04-22.md`
     plus local diffs

8. **Raw unreviewed count no longer equals active inbox**
   - after the 2026-04-23 bulk review operation, `2561` contracts still have
     `reviewed_at IS NULL`
   - those remaining rows are expired and already filtered out of active views
   - a future agent should not interpret that raw count as "2561 inbox items"

## Do Not Assume

- Do not assume Railway cron automation is fixed just because local JSON files
  now use `sh -c`.
- Do not assume Friday schedule is live.
- Do not assume the watch workflow is UI-only; the dirty tree now includes
  schema + service + cron work.
- Do not assume `reviewed_at IS NULL` is a synonym for "shows in `/inbox`";
  expired rows can remain unreviewed while being filtered off active pages.
- Do not assume `docs/handoff-2026-04-22.md` was committed; it is currently
  untracked.
- Do not assume `docs/opportunity-eval.md` is roadmap or operational truth; it
  is a working evaluation note.
- Do not assume tests are still green after the very latest uncommitted
  cron/doc/schema changes; only older verification is recorded.

## Recommended Next Actions

1. **Stabilize the worktree into explicit units before more feature work**
   - separate:
     - dashboard triage/archive/watch UX
     - watch backend/schema
     - Railway cron fix
     - tracker/doc updates

2. **Re-verify and ship the Railway cron fix deliberately**
   - inspect current live `check-batches` deployment
   - deploy the local `sh -c` wrapper change after approval
   - manually smoke-test `check-batches`
   - confirm whether weekly should remain Monday or move to Friday before
     deploying the schedule change

3. **Decide whether the watch system is ready to become a real product thread**
   - if yes:
     - verify schema push path
     - verify watch routes/tests
     - decide whether `watch-check` should become a real Railway cron service
   - if no:
     - reduce the local tree back to the dashboard/archive/watch UI slice only

4. **Only after the above, continue with GoHighLevel pipeline planning**
   - `TODOS.md` now points to GoHighLevel as the next product thread
   - but the current dirty tree already has enough unresolved surface area
     that it should not be mixed into the same commit without intent

5. **If someone asks about the inbox/dashboard split, answer from the code and
   current DB state**
   - `/inbox`: `reviewed_at IS NULL` plus active-view filters
   - main dashboard: `reviewed_at IS NOT NULL`, `promoted=false`,
     `watched=false`, plus active-view filters
   - as of 2026-04-23, active `/inbox` count is `0`; remaining unreviewed rows
     are expired only

## User Approvals Still Needed

- approval for any deployment, Railway change, cron trigger, schema push, or
  external API call
- separate approval before touching `plan.md` or `progress.md`
- separate approval before staging/committing any of the dirty-tree work
